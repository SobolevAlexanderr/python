def spl(y):
    bob = input(y).split(',')
    print (bob)
    return bob


def lenght(y):
    bob = len(y)
    return bob


def dic(x, y):
    bob = dict(zip(x, y))
    return bob



one=spl("Введите через запятую список слов: ")
count_one = lenght(one)
print("Слов в списке:", count_one)
two = spl( "введите еще один список с таким же количеством слов ")
if lenght(one) != lenght(two):
    print('ввели неверное количество слов')
    exit()
else:
    dom = dic(result, res)
print(dom)
