def spl(y):
    bob = input(y).split(',')
    print(bob)
    return bob


def lenght(y):
    bib = len(y)
    print("Слов в списке:", bib)
    return bib


def dic(x, y):
    bob = dict(zip(x, y))
    return bob


one = spl("Введите через запятую список слов:")
lenght(one)

two = spl("введите еще один список с таким же количеством слов ")
if len(one) != len(two):
    print('ввели неверное количество слов')
    exit()
else:
    dom = dic(one, two)
    print(dom)
